version: '3'
services:
  redis:
    image: redis
    command: redis-server
    volumes:
      - redis-data:/data
    ports:
      - 6379:6379
  mongo:
    image: mongo
    volumes:
      - mongo-db-data:/data/db
    ports:
      - 27017:27017
volumes:
  redis-data:
  mongo-db-data:
